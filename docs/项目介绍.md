# FireflyTools 项目交接说明

> 更新时间：2026-08-23
>
> 项目路径：`D:\Study\Projects\PythonProject\FireflyTools`
>
> 用途：删除历史对话后，为新的大模型对话恢复完整、可执行的项目上下文。本文件是工程交接材料，不是宣传介绍。

## 1. 新对话必须先做什么

接手项目后，按以下顺序操作：

1. 完整读取本文件。
2. 在项目根目录执行 Git 只读检查：

```powershell
git status --short --branch -uall
git log --oneline -8
```

3. 根据当前任务读取对应源码、测试和计划，不要先遍历或修改整个仓库。
4. 不要还原、覆盖、移动或删除无法确认来源的改动。
5. 修改功能或修复缺陷时遵循 TDD：先写失败测试，再做最小实现，最后运行相关回归与完整测试。
6. 提交前按明确路径暂存，避免把下载产物、浏览器 profile、临时切片或无关改动加入 Git。

事实优先级：

```text
当前源码与测试
  > 当前 git status / git diff
  > 本交接文档
  > docs/plans/ 与 docs/superpowers/
  > README.md
```

计划文档包含历史过程，其中“尚未实现”等描述可能已经过期。遇到冲突时，以当前源码和测试为准。

## 2. 当前最重要的 Git 状态

截至 2026-08-23，当前分支为 `codex/image-similarity-detection`，HEAD 为 `58b8684`。当前 `git status --short --branch -uall` 未显示上游跟踪信息，不要凭旧文档推断 ahead/behind。最近八个提交是：

```text
58b8684 feat(docs): 整合项目说明与用户体验改进清单
b199279 fix(hls): 按完整播放列表计算续传失败率
eb49dd5 fix(video-crawler): 导航超时后继续嗅探媒体流
6ab20dc version 0.1
dbc4f00 merge: stable video source selection
65f1bbc feat: stabilize video source selection and document runtime code
b8cd0d0 docs: plan stable video source selection
dd0816a docs: design stable video source selection
```

工作区不是干净状态，这是正在开发的图片相似度检测功能，不能擅自还原、覆盖、删除或混入无关提交。

### 2.1 正在进行的图片相似度检测功能

当前未提交改动包括：

```text
requirements.txt
tools/main.py
tools/theme_utils.py
tools/image_similarity_tool.py
tools/image_similarity/__init__.py
tools/image_similarity/models.py
tools/image_similarity/fingerprints.py
tools/image_similarity/grouping.py
tools/image_similarity/scanner.py
tools/image_similarity/recycle_bin.py
tests/test_main_window.py
tests/test_theme_utils.py
tests/test_image_similarity_fingerprints.py
tests/test_image_similarity_grouping.py
tests/test_image_similarity_scanner.py
tests/test_image_similarity_recycle_bin.py
tests/test_image_similarity_tool.py
docs/superpowers/specs/2026-08-23-image-similarity-detection-design.md
docs/superpowers/plans/2026-08-23-image-similarity-detection.md
```

功能状态：

- 主窗口已增加第 5 个“图片相似度检测”标签页。
- 已实现目录枚举、混合指纹、完全重复/视觉相似分组、结果复核、异步缩略图和安全回收站处理。
- 删除默认不勾选任何文件，只允许用户二次确认后移入系统回收站，不存在永久删除后备。
- 已修复两轮代码评审发现的问题；最近一次独立复审结论为 Ready，Critical、Important、Minor 均为 0。
- 当前实现和文档仍在工作区中，未得到用户提交指令前不要自行提交或推送。

### 2.2 当前验证状态

最近一次完整验证结果：

```text
日期：2026-08-23
Ran 233 tests in 14.681s
OK (skipped=1)
```

跳过项是 Windows 环境缺少创建符号链接权限（`WinError 1314`），不是业务失败。`git diff --check` 通过，仅出现已跟踪文件 LF 转 CRLF 的提示。文档后续若继续修改，应重新做与改动范围相称的验证，不能只引用本快照。

### 2.3 已经提交的相关历史工作

- “全部运行代码中文注释”和视频选源稳定性已经包含在 `65f1bbc`，不再是当前未提交工作。
- 导航超时后保留并继续处理已捕获媒体流的修复位于 `eb49dd5`。
- HLS 续传失败率按完整播放列表切片总数计算的修复位于 `b199279`。
- 项目说明和用户体验问题清单的整合位于 `58b8684`。

历史设计或计划中的“尚未实现”“功能源码未提交”等状态可能已经过期，必须以当前源码、测试和 Git 状态为准。

### 2.4 已知测试目录权限残留

以下目录可能在普通沙箱命令中产生“拒绝访问”警告：

```text
tests/tmpbs_9xr3j/
tests/tmpig9jv4e5/
tests/tmpn0jwtglu/
tests/tmpywtu22b5/
```

它们来自此前测试临时目录权限，不代表业务代码失败。不要枚举、移动或删除这些来源不明的目录；若用户以后明确要求清理，必须先验证绝对路径仍位于当前工作区，再按 Windows 安全规则处理。

## 3. 项目定位

FireflyTools（流萤媒体工具箱）是一个 Python 3.10+ / PyQt6 桌面媒体工具箱。

主入口：

```powershell
python -m tools.main
```

主窗口类：

```text
tools/main.py -> MediaToolboxApp
```

当前提供五个 UI 模块：

1. 视频下载爬虫：网页嗅探、MP4/HLS/DASH 下载、队列、诊断、断点恢复和错误报告。
2. 视频子目录提取：把深层目录中的视频移动到指定根目录。
3. 关键字归档：按文件名关键字整理图片和视频。
4. 图片智能裁剪：基于 Pillow 批量缩放、居中或按顶部/底部对齐裁剪。
5. 图片相似度检测：扫描目录，以混合指纹分组完全重复和视觉相似图片，由用户复核后移入系统回收站。

视频下载爬虫仍是媒体下载核心；当前工作区的主要开发任务是图片相似度检测：

```text
UI/任务队列       tools/video_downloader.py
核心编排          tools/video_crawler/spider.py
网页嗅探          tools/video_crawler/sniffer.py
会话继承          tools/video_crawler/session.py
HLS 下载          tools/video_crawler/adapters/hls.py
DASH 下载         tools/video_crawler/adapters/dash.py
断点状态          tools/video_crawler/resume.py

图片相似度 UI     tools/image_similarity_tool.py
指纹与数据模型     tools/image_similarity/fingerprints.py、models.py
候选索引与分组     tools/image_similarity/grouping.py
目录扫描          tools/image_similarity/scanner.py
安全回收站服务     tools/image_similarity/recycle_bin.py
```

## 4. 安装、启动和工作目录

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
playwright install chromium
python -m tools.main
```

`requirements.txt` 当前依赖：

- PyQt6
- Pillow
- requests
- aiohttp
- m3u8
- cryptography
- Playwright
- yt-dlp
- ImageHash
- Send2Trash
- NumPy
- SciPy

Windows FFmpeg 保存在 `tools/ffmpeg.exe`，必须保留在 Git。导入 `tools` 时，`tools/runtime_setup.py` 会将 `tools/` 放到子进程 `PATH` 前部。Linux 和 macOS 仍需自行安装 FFmpeg。

必须从项目根目录启动。以下目录都按当前工作目录解析：

```text
./downloads
./temp
./browser_profiles
```

如果从 `tools/` 目录启动，产物可能写入 `tools/downloads/`，从而出现“日志显示成功，但根目录找不到文件”的误解。

## 5. 推荐仓库结构

```text
FireflyTools/
├─ tools/
│  ├─ main.py
│  ├─ runtime_setup.py
│  ├─ theme_utils.py
│  ├─ video_downloader.py
│  ├─ video_extractor.py
│  ├─ keyword_organizer.py
│  ├─ image_resizer.py
│  ├─ image_similarity_tool.py
│  ├─ ffmpeg.exe
│  ├─ image_similarity/
│  │  ├─ __init__.py
│  │  ├─ models.py
│  │  ├─ fingerprints.py
│  │  ├─ grouping.py
│  │  ├─ scanner.py
│  │  └─ recycle_bin.py
│  └─ video_crawler/
│     ├─ models.py
│     ├─ errors.py
│     ├─ logging_utils.py
│     ├─ reporting.py
│     ├─ session.py
│     ├─ resume.py
│     ├─ diagnostics.py
│     ├─ sniffer.py
│     ├─ spider.py
│     └─ adapters/
│        ├─ base.py
│        ├─ direct_mp4.py
│        ├─ hls.py
│        ├─ dash.py
│        └─ ytdlp.py
├─ tests/
├─ docs/
│  ├─ 项目介绍.md
│  ├─ 工具箱用户体验问题清单.md
│  ├─ plans/
│  └─ superpowers/
├─ pic/
├─ requirements.txt
├─ README.md
└─ LICENSE
```

`pic/` 当前有 8 张内置壁纸，全部是 UI 外观依赖，必须保留在 Git。

`.gitignore` 已忽略：

```text
.venv/  venv/  env/
__pycache__/  *.py[cod]
downloads/  temp/  browser_profiles/
tests/.tmp/
.worktrees/
*.part  *.tmp  .firefly-segments.json*
```

被忽略不代表可以随意删除。下载产物、未完成切片和持久化浏览器 profile 可能对用户有价值。

## 6. UI 当前实现

`tools/main.py` 启动后注册五个标签页。

主窗口特性：

- 无边框圆角窗口。
- 自定义标题栏支持最小化、最大化、关闭和壁纸切换。
- 右下角 `QSizeGrip` 支持拖动缩放。
- `pic/` 中所有 JPG/PNG 都作为候选壁纸。
- `tools/theme_utils.py` 根据壁纸亮度生成动态 QSS 和弹窗主题。

视频下载页：

- 使用透明 `QScrollArea`，窗口较小时可滚动，壁纸仍可透出。
- 下载参数和嗅探参数分两行，避免“切片并发”一行控件堆叠。
- 支持稳定/高速模式和自定义切片并发数。
- 支持断点续传、yt-dlp 后备、直播录制秒数。
- 支持无头或可视化 Playwright 嗅探。
- 支持持久化 Chromium profile 和人工等待时间。
- 支持“诊断链接”、任务队列、批次完成弹窗和可恢复失败重试。

任务加入队列时会快照全部设置，因此用户后来修改 UI 不会影响已经排队的任务。

后台线程通过 Qt 信号更新日志、队列显示和批次结果，避免直接从工作线程操作控件。

图片相似度检测页：

- 使用透明根控件、透明滚动区和半透明悬浮面板，沿用动态壁纸主题，不用不透明整页背景遮挡壁纸。
- 选择单个扫描目录；可控制是否递归子目录，默认递归。
- 提供严格、标准、宽松三档预设，默认严格，不向普通用户暴露原始距离阈值。
- 扫描阶段依次报告枚举、视觉指纹、完整 SHA-256 校验、分组和完成状态；普通进度最多每秒更新 10 次。
- 左侧显示完全重复/视觉相似分组，右侧显示图片、路径、尺寸、格式、大小和“建议保留”标记。
- 所有回收站复选框默认未选中；“保留建议项并选择本组其他图片”只有用户主动点击才会执行。
- 支持打开图片、打开所在文件夹和复制路径。
- 缩略图按需后台解码，使用容量 128 的 LRU 缓存和有界延迟队列；`QPixmap` 只在主线程创建。
- 删除前显示所选数量、总大小和完整路径并二次确认；确认按钮不是默认按钮。
- 回收操作、重新分组或扫描期间禁用冲突操作，逐项失败不会阻断其他项目。

图片相似度核心流程：

```text
安全枚举普通图片（不跟随符号链接/reparse point）
  -> 最多 4 个线程读取、EXIF 方向修正并计算视觉指纹
  -> 仅对同大小候选计算 SHA-256，识别完全重复
  -> pHash + dHash 生成候选
  -> 宽高比 + 16×16 灰度 + 8×8 预乘 RGBA 精确复核
  -> BK-tree / SciPy cKDTree 下界索引缩小候选集
  -> 只按代表图直接匹配生成稳定分组，避免链式扩组
  -> 用户人工选择
  -> 再次核对路径、文件快照和文件身份
  -> Send2Trash 移入系统回收站
  -> 移除成功项并后台重新分组
```

安全边界：

- 扫描过程只读，取消后返回 `cancelled=True` 且不暴露半成品分组。
- 支持 JPG/JPEG、PNG、BMP、WebP、TIF/TIFF；损坏、超大、无权限或扫描中变化的图片按单项结构化失败处理。
- 指纹前后、SHA-256 后和移入回收站前均复核大小、`mtime_ns` 及可用的设备/文件身份。
- 回收站服务只接受本次正式扫描结果、扫描根目录内且未变化的普通文件。
- 不调用 `unlink()`、`remove()`，回收站后端失败时也不降级为永久删除。
- `send2trash` 是路径型后端，无法绝对消除“最终复核后到系统调用前”的极短竞态窗口；当前实现已把最终路径和身份复核放到后端调用前的最近位置。
- 主要目标规模是一万张图片以内；性能测试覆盖哈希碰撞、等亮度异色和随机透明度等密集候选，最终仍以原始描述符精确复核保证阈值内候选不漏报。

相关测试：

```text
tests/test_main_window.py
tests/test_theme_utils.py
tests/test_video_downloader.py
tests/test_image_similarity_fingerprints.py
tests/test_image_similarity_grouping.py
tests/test_image_similarity_scanner.py
tests/test_image_similarity_recycle_bin.py
tests/test_image_similarity_tool.py
```

## 7. 爬虫核心模型

文件：`tools/video_crawler/models.py`

主要类型：

- `MediaKind`：`DIRECT_MP4`、`HLS`、`DASH`、`DRM`、`UNKNOWN`。
- `MediaCandidate`：候选 URL、类型、来源、可信分数、Content-Type、切片数、带宽等。
- `BrowserSessionSnapshot`：User-Agent、Referer、Origin、Cookie、允许继承的 Header、LocalStorage。
- `PageAccessSnapshot`：主页状态码、标题、最终 URL、video/iframe 数量。
- `SnifferOptions`：无头/可视化、持久 profile、等待秒数。
- `DiagnosticReport`：候选流、会话、警告和错误。

重要区分：

- `MediaCandidate.score` 表示候选证据可信度，不代表视频画质。
- `segment_count` 更接近内容时长线索，不代表画质。
- `bandwidth` 和分辨率才是画质排序的重要指标。
- LocalStorage 会被记录，但当前不会自动全部转换为下载请求头。

结构化异常位于 `tools/video_crawler/errors.py`：

- `VideoErrorCode`：稳定错误码。
- `VideoDownloadError`：错误码、用户消息、详情和 `retryable`。

## 8. 适配器架构

文件：`tools/video_crawler/adapters/base.py`

`VideoAdapter` Protocol 约定：

```text
can_handle(candidate)
download(candidate, output_filename)
diagnose(url)
```

`VideoDownloadOrchestrator` 先让候选解析器返回统一 `MediaCandidate`，再按优先级选适配器：

```text
DirectMp4Adapter  100
HlsAdapter         80
DashAdapter        60
```

`YtDlpAdapter` 是 UI 控制的外部后备，不在默认 orchestrator 列表中。

这种拆分使 UI 不需要理解 HLS AES，HLS 下载器也不需要知道 PyQt 控件。

## 9. 一次下载任务的完整流程

```text
VideoDownloaderTool.add_to_queue()
  -> 把 URL、保存名、目录和所有选项保存为 task 快照
  -> queue.Queue
  -> 后台 queue_worker()
  -> VideoDownloaderTool._execute_task()
  -> 创建 UniversalVideoSpider
  -> UniversalVideoSpider.run()
  -> VideoDownloadOrchestrator.download()
  -> _resolve_candidate()
       ├─ MP4 直链  -> DirectMp4Adapter
       ├─ M3U8 直链 -> HlsAdapter
       ├─ MPD 直链  -> DashAdapter
       └─ 普通网页  -> PageSniffer
                        -> DiagnosticReport
                        -> 验证并选择真实媒体候选
  -> 对应适配器下载
  -> 验证输出存在且非空
  -> 返回 output_path 或抛出 VideoDownloadError
  -> UI 汇总批次结果
```

`tools/video_downloader.py::_execute_task()` 是 UI 与爬虫核心的主要边界。

`tools/video_crawler/spider.py::UniversalVideoSpider.run()` 是下载编排入口。

## 10. Playwright 网页嗅探

文件：`tools/video_crawler/sniffer.py`

`PageSniffer` 支持：

- 无头或可视化 Chromium。
- 可选持久化 profile。
- 页面状态码、标题、最终 URL、video/iframe 数量诊断。
- 监听页面 response 事件。
- 根据 URL 和 Content-Type 分类 MP4、M3U8、MPD、DRM。
- 从 JSON、HTML、JavaScript 响应正文提取媒体 URL。
- 还原 `\/`、`\u002F`、`\u0026` 等转义。
- 过滤 `https://host/\.m3u8` 这类并非真实地址的正则模板。
- 模拟点击 video 或页面中心以触发媒体请求。
- 捕获浏览器 Cookie、请求 Header 和 LocalStorage。
- 在 `page.goto()` 导航超时或页面加载异常时保留已捕获候选，并继续完成剩余观察与候选探测；导航超时本身不再等价于“未发现媒体”。

等待规则：

- 只有真实 network HLS 是最高优先级终止证据，可以提前结束等待。
- network MP4、network DASH 和所有 response-body 候选都不能提前结束；若没有 HLS，继续等待到 `manual_wait_seconds` 上限。
- 只在响应正文中发现 URL 字符串不算可靠证据，仍会继续等待播放器实际请求。
- 等待结束日志会说明是捕获 network HLS 还是达到时间上限，并输出按类型/来源统计的候选数量。
- 主页返回 403 或标题明显表示访问受限时，抛出 `HTTP_FORBIDDEN`。

持久化 profile 默认路径：

```text
./browser_profiles/video_crawler
```

该目录可能包含 Cookie 和站点存储，禁止加入 Git。

## 11. 浏览器会话继承

文件：`tools/video_crawler/session.py`

下载请求会继承：

- 浏览器 User-Agent。
- 来源页面 Referer。
- 页面 Origin。
- 与目标域名匹配的 Cookie。
- allowlist 中的 Authorization、Accept、Accept-Language、Range、X-Token、X-Auth-Token。

不会把任意捕获请求头全部转发，避免跨域泄露敏感信息。

浏览器会话不仅用于最终媒体下载，也用于 M3U8 playlist 探测和 MP4 HEAD/Range 元数据探测。否则某些 CDN 会对缺少 Referer、Origin 或 Cookie 的 Python 请求返回 403，即使浏览器内已经成功播放。

所有日志、诊断和弹窗应经过 `redact_for_display()`，隐藏 Cookie、Authorization、token、signature 等内容。

## 12. M3U8 正片与画质选择

入口：`tools/video_crawler/spider.py::_select_best_m3u8()`

网页可能同时暴露广告、预告、多个 CDN 地址和多个清晰度。当前选择分两步：

1. 探测每个候选的有效性和切片数。
2. 在接近最大切片数的候选中，按带宽、分辨率像素数和切片数排序。

正片容差：

```text
max(3, 最大切片数的 5%)
```

如果候选是 master playlist，先读取最高带宽/分辨率 variant，再统计 media playlist 的切片。

直接 media playlist 还会从 `/3000k/` 等 URL 片段推断码率。

playlist 探测使用 15 秒超时，并按每个候选域名继承浏览器会话 Header。该值高于旧版 5 秒：现场目标的 playlist 在同一环境中曾约 8.9 秒才返回 200，5 秒会把可用正片误判为网络超时。

因此：

- 切片数量相同不代表画质相同。
- 三个候选均为约 368 个切片时，不会简单选择第一个。
- 若所有候选无法验证，不会再使用“最后捕获的 M3U8”，而是返回 `NETWORK_TIMEOUT` 或 `M3U8_PARSE_FAILED`。

## 13. MP4、HLS、DASH 和 yt-dlp

### 13.1 Direct MP4

文件：`tools/video_crawler/adapters/direct_mp4.py`

- 使用 `requests.get(stream=True)`。
- 以 1 MiB chunk 流式写入，避免把大视频全部载入内存。
- 有 Content-Length 时记录下载进度。
- 最终验证输出文件存在且非空。

网页嗅探降级到 MP4 前还会执行候选验证：

- 同一 URL 的 response-body/network 记录先去重并升级证据来源。
- 使用 HEAD 获取 Content-Length；HEAD 不支持或缺少大小时，用单字节 Range 请求读取 Content-Range 总长度。
- 不设置固定最小文件大小，避免误伤合法短视频；大小仅用于多个候选之间比较。
- response-body-only MP4 若无法验证，不会直接下载。

### 13.2 HLS

文件：`tools/video_crawler/adapters/hls.py`

当前主路径支持：

- master/media playlist。
- 最高带宽 variant 选择。
- TS 切片。
- fMP4 和 `EXT-X-MAP` 初始化片段。
- AES-CBC、显式 IV 和基于 media sequence 的默认 IV。
- `EXT-X-BYTERANGE` 与 HTTP Range。
- asyncio/aiohttp 并发下载、Semaphore 限流、重试与退避。
- 默认音轨和 WebVTT 字幕下载与 mux。
- 无 `ENDLIST` 直播 playlist 的定时录制。
- `.firefly-segments.json` 切片级断点续传。
- 失败切片不超过完整媒体播放列表切片总数的 3% 时继续合并；续传时分母仍是全部切片，而不是本轮待下载或失败切片数。
- FFmpeg TS 合并、fMP4 容器修复和多轨 mux。

重要边界：

`group_fmp4_segments()`、`ordered_ts_segments()` 等 helper 和测试已经表达多 init map、DISCONTINUITY 的分组规则，但当前主下载路径尚未覆盖 HLS 标准允许的所有复杂组合。不要仅根据 helper 测试就断言复杂 playlist 已完全端到端支持。

### 13.3 DASH

文件：`tools/video_crawler/adapters/dash.py`

当前支持：

- 无 DRM 的静态 MPD。
- `Period -> AdaptationSet -> Representation` 解析。
- 最高带宽视频轨和音频轨。
- `SegmentTemplate` 的 `$RepresentationID$`、`$Number$` 展开。
- 初始化分段和媒体分段下载。
- video/audio 各自的 manifest 断点恢复。
- FFmpeg 将独立音视频轨 mux 为 MP4。

当前不支持：

- 动态或直播 MPD。
- `SegmentTimeline`。
- DRM 绕过。
- 缺少音轨或视频轨的复杂 MPD。
- DASH 标准中的全部 BaseURL 和模板变体。

### 13.4 yt-dlp

文件：`tools/video_crawler/adapters/ytdlp.py`

仅当以下条件同时成立时调用：

1. 用户启用了 yt-dlp 后备。
2. 内置路径错误码是 `NO_MEDIA_FOUND` 或 `UNSUPPORTED_DASH`。
3. 系统能找到 `yt-dlp`。

403、网络超时、HLS 解析失败和 DRM 不会无条件转交 yt-dlp。

## 14. 断点续传

文件：`tools/video_crawler/resume.py`

`SegmentManifest` 保存每个切片的 URL 和大小。恢复时需要同时满足：

- 切片文件存在。
- 文件大小大于 0。
- manifest 中存在记录。
- 当前大小与记录大小一致。

HLS 失败且启用恢复时保留临时切片；成功后才清理。DASH 的 video/audio 轨各自维护 manifest。

尚未完全解决的风险：

`SegmentManifest.save()` 使用固定 `.firefly-segments.json.tmp` 写入后调用 `os.replace()`。Windows 文件占用或安全软件扫描可能导致 WinError 5。后续修复应先写测试，再考虑唯一临时文件名、PermissionError 重试和结构化文件访问错误。

## 15. FFmpeg 与输出验证

FFmpeg 主要用于 mux/remux，不是网页嗅探器：

- TS 切片通过 concat demuxer 合并。
- fMP4 先拼接 init 和媒体片段，再用 FFmpeg 修复容器。
- DASH 独立视频轨、音频轨通过 `-c copy` mux。
- HLS 独立音频、字幕通过 `-map` 选择轨道。

`-c copy` 表示尽量不重新编码。

每条成功路径都应验证最终文件存在且大小大于 0，否则返回 `EMPTY_OUTPUT`。

FFmpeg 成功后偶尔出现：

```text
WinError 145: 目录不是空的
```

这通常是非致命清理警告。先确认最终输出验证结果，不要让清理异常覆盖已成功生成的视频。

## 16. 错误码与重试语义

```text
UNKNOWN
NETWORK_TIMEOUT
HTTP_FORBIDDEN
HTTP_NOT_FOUND
NO_MEDIA_FOUND
UNSUPPORTED_DASH
UNSUPPORTED_DRM
M3U8_PARSE_FAILED
SEGMENT_FAILURE_RATE_EXCEEDED
FFMPEG_FAILED
EMPTY_OUTPUT
```

新增失败路径应优先抛出 `VideoDownloadError`，不要让 requests、aiohttp、m3u8、subprocess 或 WinError 直接落入 `UNKNOWN`。

UI 只对 `retryable=True` 的任务显示直接重试入口。不能通过重试解决的权限、DRM、资源类型或 FFmpeg 配置问题应先诊断原因。

## 17. 安全和合法边界

项目明确不做：

- Widevine、FairPlay、PlayReady 或其他 DRM 绕过。
- 验证码破解。
- 登录、付费墙、账号权限或地区限制突破。
- 恶意高并发请求。
- 敏感 Cookie、Authorization、token 或浏览器 profile 上传 Git。

可视化嗅探只允许用户在浏览器中完成站点许可的人工操作，并不保证绕过访问策略。

可视化模式遇到安全验证页时，会在同一浏览器上下文中保留配置的等待时间；
用户完成人工验证后，嗅探器重新读取当前页面状态、触发播放器并监听媒体请求。
无界面模式仍直接返回 `HTTP_FORBIDDEN`。首次导航的 403 只表示初始验证页，
不会覆盖人工验证后真实播放器的最终状态。

“系统 Chrome（实验）”会让 Playwright 使用本机安装的 Google Chrome；启用
“复用浏览器会话”时，它使用独立的 `browser_profiles/video_crawler_chrome`
profile，不与随附 Chromium 的会话目录混用。该模式只解决浏览器版本或内核
兼容性问题，不隐藏 `navigator.webdriver`，也不保证通过站点自动化检测。

## 18. 近期调试结论

- `kanav.ad` 页面会同时暴露多个广告/推荐 MP4 和一个延迟出现的正片 HLS。旧版因 `networkidle` 常等待到 25 秒超时而偶然捕获 HLS；新版曾在首个 network MP4 出现后立即关闭浏览器，形成时序竞态。
- 当前通过“仅 network HLS 可提前终止 + 其他候选等满观察窗口”消除该竞态，不依赖 `networkidle` 超时作为正常控制流。
- `taiav.com` 等页面可能在 `page.goto(..., wait_until="networkidle")` 超时时已经捕获 M3U8。当前把导航异常作为警告，继续使用已捕获候选，不再直接返回 `NO_MEDIA_FOUND`。
- 正片 HLS `cdn16.11yun.space/GAV1/328752/328752.m3u8` 在缺少浏览器来源头时现场返回 403；继承 Referer/Origin/Cookie 后可返回 200 并识别 266 个切片。
- 同一 playlist 在现场曾约 8.9 秒返回，旧 5 秒候选探测超时不足；当前统一使用 15 秒。
- `https://www.aowu.tv/\.m3u8` 是脚本正则模板，不是真实流，当前已过滤。
- 旧逻辑会在 M3U8 探测全部失败后继续下载并最终报 WinError 10060；当前在候选选择阶段返回结构化超时或解析错误。
- 切片数量用于识别正片，不代表画质；当前同时使用带宽、分辨率和 URL 码率线索。
- 输出目录取决于进程 CWD。排查“成功但找不到文件”时，先检查启动目录、UI 保存路径和日志中的绝对路径。
- 浏览器中能播放不等于 Python 请求必然可下载，媒体服务器仍可能检查 Cookie、Referer、Origin、Authorization 或短期 token。
- HLS 续传会复用上次成功切片。失败率计算必须使用“本轮最终失败切片数 ÷ 完整播放列表切片数”；例如 412 个切片中只有 2 个失败时约为 0.49%，不能错误地用上次失败的 8 个切片作分母得到 25%。

## 19. 当前测试基线

完整命令：

```powershell
$env:QT_QPA_PLATFORM='offscreen'
$env:PYTHONDONTWRITEBYTECODE='1'
python -m unittest discover -s tests -v
```

最近完整验证：

```text
日期：2026-08-23
Ran 233 tests in 14.681s
OK (skipped=1)
```

跳过项是 Windows 当前进程没有创建符号链接权限（`WinError 1314`），不代表功能失败。该数字是当前未提交图片相似度实现完成两轮修复后的最近基线；继续改动源码后必须重新运行，不能把它当作永久结论。

测试覆盖：

- 主窗口尺寸、壁纸和拉伸。
- 动态主题与 QMessageBox 可读性。
- 下载 UI、任务快照、队列、批次结果和重试。
- 适配器选择与 spider 编排。
- 页面访问受限、可视化和持久会话嗅探。
- 响应正文候选提取和伪 URL 过滤。
- 广告 MP4 先到、正片 HLS 后到的确定性事件序列回归。
- 候选 URL 去重与 response-body 到 network 的证据升级。
- MP4 HEAD/Range 大小探测、浏览器会话继承和顺序无关排序。
- 慢速 M3U8 playlist 的 15 秒探测预算与会话 Header 继承。
- Cookie、Header、LocalStorage 会话模型。
- 日志和诊断脱敏。
- HLS AES IV、BYTERANGE、fMP4 helper、音轨、字幕和直播。
- HLS 断点续传、失败比例和 FFmpeg 错误。
- DASH MPD、DRM 拒绝、分段恢复和 mux。
- yt-dlp 后备。
- 全部运行模块与具名定义的 docstring 覆盖。
- 图片 EXIF 方向修正、白底感知哈希、灰度及预乘 RGBA 混合指纹。
- 完全重复 SHA-256、三档阈值、直接代表匹配和建议保留排序。
- BK-tree 与灰度/颜色 cKDTree 下界索引，包括一万张密集碰撞性能回归。
- 递归开关、链接/reparse point 拒绝、损坏图片隔离、稳定顺序、取消和进度节流。
- 删除前路径边界、文件快照、文件身份和最终复核；测试只使用可注入假回收站后端。
- 图片相似度页默认值、无自动勾选、二次确认、后台线程、缩略图缓存、重分组和动态主题。

常用局部测试：

```powershell
python -m unittest tests.test_video_crawler_models -v
python -m unittest tests.test_video_crawler_sniffer_access tests.test_video_crawler_session -v
python -m unittest tests.test_video_crawler_hls tests.test_video_crawler_hls_renditions tests.test_video_crawler_hls_live -v
python -m unittest tests.test_video_crawler_dash tests.test_video_crawler_dash_resume -v
$env:QT_QPA_PLATFORM='offscreen'
python -m unittest tests.test_video_downloader -v
python -m unittest tests.test_image_similarity_fingerprints tests.test_image_similarity_grouping tests.test_image_similarity_scanner tests.test_image_similarity_recycle_bin -v
$env:QT_QPA_PLATFORM='offscreen'
python -m unittest tests.test_image_similarity_tool tests.test_main_window tests.test_theme_utils -v
```

## 20. 测试文件地图

```text
tests/test_code_documentation.py              注释/docstring 覆盖
tests/test_main_window.py                     主窗口和 QSizeGrip
tests/test_runtime_setup.py                   bundled FFmpeg
tests/test_theme_utils.py                     动态主题和弹窗
tests/test_video_downloader.py                UI、队列、spider 兼容行为
tests/test_video_crawler_models.py            模型和结构化错误
tests/test_video_crawler_adapters.py          适配器与编排器
tests/test_video_crawler_diagnostics.py       静态诊断和候选提取
tests/test_video_crawler_sniffer_access.py    访问受限与嗅探等待
tests/test_video_crawler_session.py           浏览器会话继承
tests/test_video_crawler_redaction.py         敏感信息脱敏
tests/test_video_crawler_resume.py            SegmentManifest
tests/test_video_crawler_hls.py               HLS helper
tests/test_video_crawler_hls_renditions.py    HLS 音轨和字幕
tests/test_video_crawler_hls_live.py          HLS 直播
tests/test_video_crawler_structured_errors.py HLS/FFmpeg 错误
tests/test_video_crawler_dash.py              DASH 解析和下载
tests/test_video_crawler_dash_resume.py       DASH 断点恢复
tests/test_video_crawler_ytdlp.py             yt-dlp 后备
tests/test_image_similarity_fingerprints.py   数据模型、文件快照和混合指纹
tests/test_image_similarity_grouping.py       完全重复、视觉分组和一万张性能回归
tests/test_image_similarity_scanner.py        安全枚举、并发、进度和取消
tests/test_image_similarity_recycle_bin.py    删除前复核和假回收站后端
tests/test_image_similarity_tool.py           页面默认值、选择、缩略图和二次确认
```

## 21. 文档地图

`docs/项目介绍.md`

- 当前的新对话交接文档。
- 优先用于恢复项目状态和工作纪律。

`docs/工具箱用户体验问题清单.md`

- 从普通用户视角整理当前工具箱最需要解决的问题、建议方案和验收标准。
- 仅作为后续改进依据，不代表相关功能已经实现。

`docs/plans/video-crawler-phased-implementation-plan.md`

- 原始 Phase 0-9 计划，Phase 1-9 已执行。

`docs/plans/video-crawler-gap-closure-implementation-plan.md`

- 缺口补齐 Phase A-G，已执行。

`docs/plans/video-crawler-access-limited-sniffing-implementation-plan.md`

- 访问受限嗅探 Phase A-E，已执行。

`docs/plans/video-crawler-roadmap.md`

- 早期路线图，状态可能过期。

`docs/superpowers/`

- 设计和实施记录。
- 不是运行依赖，但有助于理解功能决策和未完成工作。

`docs/superpowers/specs/2026-07-03-video-source-selection-stability-design.md`

- 视频选源稳定性修复的设计规格。
- 记录等待竞态、候选归一化、MP4 验证和不恢复盲目 M3U8 fallback 的决策。

`docs/superpowers/plans/2026-07-03-video-source-selection-stability.md`

- 视频选源稳定性修复的 TDD 实施计划。
- 对应计划提交 `b8cd0d0`；功能源码和运行代码中文注释已包含在 `65f1bbc`。

`docs/superpowers/specs/2026-08-23-image-similarity-detection-design.md`

- 图片相似度检测第一版设计规格。
- 记录只读扫描、混合指纹、人工选择、系统回收站、二次确认、一万张规模和透明动态主题等产品决策。

`docs/superpowers/plans/2026-08-23-image-similarity-detection.md`

- 图片相似度检测的 TDD 实施计划。
- 当前实现已经落在工作区，但尚未提交；计划开头的 BK-tree 简述不足以覆盖最新实现，密集候选还使用 SciPy cKDTree 下界投影，最终以精确原始描述符复核。

## 22. 按任务定位文件

```text
下载 UI、队列、弹窗
  tools/video_downloader.py
  tests/test_video_downloader.py

主窗口、壁纸、缩放和主题
  tools/main.py
  tools/theme_utils.py
  tests/test_main_window.py
  tests/test_theme_utils.py

媒体模型和结构化错误
  tools/video_crawler/models.py
  tools/video_crawler/errors.py
  tests/test_video_crawler_models.py

403、可视化、持久 profile、响应正文候选
  tools/video_crawler/sniffer.py
  tools/video_crawler/session.py
  tools/video_crawler/diagnostics.py
  tests/test_video_crawler_sniffer_access.py
  tests/test_video_crawler_session.py

网页等待、候选去重、广告 MP4 先到
  tools/video_crawler/sniffer.py
  tests/test_video_crawler_sniffer_access.py

M3U8 候选、会话探测、正片和画质选择
  tools/video_crawler/spider.py
  tools/video_crawler/adapters/hls.py
  tests/test_video_downloader.py

MP4 后备验证和排序
  tools/video_crawler/spider.py
  tools/video_crawler/session.py
  tests/test_video_downloader.py

HLS 解密、BYTERANGE、直播、断点和合并
  tools/video_crawler/adapters/hls.py
  tools/video_crawler/resume.py
  tests/test_video_crawler_hls*.py

DASH
  tools/video_crawler/adapters/dash.py
  tools/video_crawler/resume.py
  tests/test_video_crawler_dash*.py

yt-dlp 后备
  tools/video_crawler/adapters/ytdlp.py
  tests/test_video_crawler_ytdlp.py

注释工作
  tools/**/*.py
  tests/test_code_documentation.py
  docs/superpowers/specs/2026-06-27-runtime-code-comments-design.md
  docs/superpowers/plans/2026-06-27-runtime-code-comments.md

图片相似度页面、选择、缩略图和主题
  tools/image_similarity_tool.py
  tools/main.py
  tools/theme_utils.py
  tests/test_image_similarity_tool.py
  tests/test_main_window.py
  tests/test_theme_utils.py

图片指纹、阈值和文件快照
  tools/image_similarity/models.py
  tools/image_similarity/fingerprints.py
  tests/test_image_similarity_fingerprints.py

图片完全重复、视觉相似分组和性能
  tools/image_similarity/grouping.py
  tests/test_image_similarity_grouping.py

图片目录安全枚举、并发、取消和进度
  tools/image_similarity/scanner.py
  tests/test_image_similarity_scanner.py

图片回收站安全复核
  tools/image_similarity/recycle_bin.py
  tests/test_image_similarity_recycle_bin.py
```

## 23. 接下来最可能的工作

如果继续当前图片相似度检测功能：

1. 重新执行 `git status --short --branch -uall` 和 `git log --oneline -8`，确认本节没有被更新后的提交状态淘汰。
2. 阅读 2026-08-23 的图片相似度设计规格、实施计划，以及 `tools/image_similarity_tool.py`、`tools/image_similarity/` 和五个对应测试文件。
3. 先检查完整差异，不要覆盖已完成的两轮评审修复。
4. 修改算法、文件安全或线程生命周期前先写失败测试；相似分组不得退化为一万张图片的全量平方比较，也不得用截断候选换取速度。
5. 运行五个图片相似度测试、主窗口/主题测试、`tests/test_code_documentation.py` 和完整测试。
6. 有图形环境时可做一次人工 UI 冒烟：透明背景、扫描/取消、分组切换、默认无选择、完整路径二次确认、回收后重分组。不要用重要原图验证回收站流程。
7. 用户明确要求提交时，再按图片相似度功能的明确路径暂存；不要把来源不明测试临时目录加入 Git。

如果继续视频爬虫：

1. 重点保留 `eb49dd5` 的导航超时韧性和 `b199279` 的完整播放列表失败率口径。
2. 改动嗅探、候选选择或 HLS 续传前先运行对应聚焦测试。
3. 必要时只做嗅探/playlist 探测验证，不要为验证而下载完整视频。

如果开始新功能或缺陷修复：

1. 先定位对应源码和测试。
2. 检查目标文件是否与当前图片相似度未提交改动重叠，必须在现有改动之上继续工作。
3. 写失败测试复现需求。
4. 做最小实现。
5. 运行相关测试和完整测试。
6. 检查日志是否脱敏、错误是否结构化、临时文件是否可恢复。

## 24. Git 和修改纪律

- 不执行 `git reset --hard`。
- 不执行 `git checkout -- <file>` 或等价还原，除非用户明确要求。
- 不删除来源不明的未跟踪文件。
- 不把下载产物、临时切片、浏览器 profile、虚拟环境或缓存提交 Git。
- `tools/ffmpeg.exe` 必须保留在 Git。
- `pic/` 中 8 张壁纸必须全部保留在 Git。
- 不因测试临时目录 PermissionError 修改业务逻辑。
- 修改含用户改动的文件前先读 diff，并在现有改动上继续。
- 提交前使用 `git diff --check`，并按明确路径 `git add -- <paths>`。

## 25. 新对话最短提示词

```text
请先完整读取 D:\Study\Projects\PythonProject\FireflyTools\docs\项目介绍。
这是一个 Python + PyQt6 媒体工具箱，包含视频下载爬虫和图片相似度检测等五个工具页。
视频队列入口在 tools/video_downloader.py，爬虫核心在 tools/video_crawler/。
图片相似度 UI 在 tools/image_similarity_tool.py，核心在 tools/image_similarity/。
当前工作区包含尚未提交的图片相似度检测功能，不能还原、覆盖或混入无关改动。
开始前执行 git status --short --branch -uall 和 git log --oneline -8。
随后根据任务读取对应源码、测试、docs/plans/ 和 docs/superpowers/；不要删除来源不明的文件。
```

## 26. 一句话心智模型

```text
MediaToolboxApp 组合五个透明主题工具页
  ├─ 视频下载：UI 快照队列任务
  │    -> UniversalVideoSpider / PageSniffer 解析媒体并捕获会话
  │    -> Orchestrator 选择 MP4/HLS/DASH 适配器
  │    -> 下载、恢复、FFmpeg 合并
  │    -> 结构化错误或有效输出返回批次结果
  └─ 图片相似度：安全枚举与混合指纹
       -> 索引化候选过滤和代表图直接匹配分组
       -> 用户人工选择并核对数量、大小、完整路径
       -> 最终文件复核后移入系统回收站
       -> 移除成功项并重新分组
```

## 27. Edge 辅助捕获（Stage 1）

当真实视频能在用户自己的普通 Microsoft Edge 中播放、但站点限制 Playwright 自动化浏览器时，可以使用 `browser_extensions/edge_video_capture`。完整安装、操作、自动化证据和待人工验收项见 [Edge 辅助视频捕获 README](../browser_extensions/edge_video_capture/README.md)。

这是用户授权的“观察当前选中标签页并转交候选”流程，不是 stealth、自动化伪装或人机验证绕过。Stage 1 通过剪贴板闭环：扩展只在用户点击开始后申请当前网页/CDN host 权限，捕获单个标签页的 HLS/MP4/DASH 候选；用户复制候选 JSON，在 FireflyTools 中点击“粘贴 Edge 候选”，复核脱敏确认框后加入现有队列。Edge 候选与普通 URL/Playwright 输入互斥，候选 5 分钟后过期，Stop 会立即清空所选标签页候选。

隐私边界必须保持：不读取或转交 Cookie、Authorization，不显示原始 token 查询值，不处理 DRM。依赖敏感会话的下载可能返回 401/403，此时只保留非敏感诊断并失败，不能通过扩展协议或导出敏感 Header 来补救。真实 Edge/目标站验收保留为最终发布门禁，当前状态以扩展 README 的“Stage 1 人工验收记录”为准。
