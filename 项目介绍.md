# FireflyTools 项目介绍

> 给新对话使用的项目交接说明。  
> 建议新对话开始后，先让 Codex 读取本文件，再按需读取 `README.md`、`plan/video-crawler-roadmap.md`、`tools/video_downloader.py` 和 `tests/test_video_downloader.py`。

## 1. 项目定位

FireflyTools（流萤媒体工具箱）是一个基于 Python + PyQt6 的桌面工具箱项目，主打媒体处理和自动化整理。应用采用无边框圆角窗口、壁纸背景、动态 QSS 主题和多线程/异步任务处理，尽量避免长耗时任务阻塞界面。

当前主窗口入口在 `tools/main.py`，运行后有四个核心页面：

1. 视频下载爬虫；
2. 视频子目录提取；
3. 关键字归档；
4. 图片智能裁剪。

项目目前最活跃、改动最多的是“视频下载爬虫”模块。

## 2. 技术栈

- Python 3.x；
- PyQt6：桌面 UI；
- Pillow：图片处理和主题色提取；
- requests / aiohttp：同步与异步网络请求；
- m3u8：HLS playlist 解析；
- cryptography：AES 解密；
- Playwright：无头浏览器网页嗅探；
- FFmpeg：视频切片合并、容器修复和转封装；
- unittest：当前测试框架。

安装依赖可参考 `README.md`，大致包括：

```bash
pip install PyQt6 Pillow requests aiohttp m3u8 cryptography playwright
playwright install chromium
```

## 3. 主要目录和文件

```text
FireflyTools/
├─ tools/
│  ├─ main.py                  # 主窗口、标签页、壁纸切换、动态主题应用
│  ├─ theme_utils.py           # 动态 QSS、阴影、明暗主题和 QMessageBox 样式
│  ├─ video_downloader.py      # 视频下载爬虫核心与 UI
│  ├─ video_extractor.py       # 视频子目录提取工具
│  ├─ keyword_organizer.py     # 关键字归档工具
│  ├─ image_resizer.py         # 图片智能裁剪工具
│  ├─ ffmpeg.exe               # 当前仓库内置的 FFmpeg 可执行文件
│  └─ video_downloader-0.1.py  # 旧版视频下载器备份
├─ tests/
│  ├─ test_video_downloader.py # 视频爬虫与队列行为测试
│  └─ test_theme_utils.py      # 动态主题/QMessageBox 样式测试
├─ docs/superpowers/
│  ├─ specs/                   # 已完成需求设计文档
│  └─ plans/                   # 已完成实施计划文档
├─ plan/
│  └─ video-crawler-roadmap.md # 视频爬虫未来路线图
├─ pic/                        # UI 壁纸素材
└─ README.md                   # 项目说明
```

## 4. 四个核心功能

### 4.1 视频下载爬虫

文件：`tools/video_downloader.py`

这是当前最重要的模块。它不是按固定网站白名单解析，而是一个通用 MP4 / M3U8 嗅探下载器。

当前能力：

- 直接下载 `.mp4` 地址；
- 直接解析和下载 `.m3u8` HLS 地址；
- 对普通网页使用 Playwright 打开页面并监听网络请求，自动捕获真实 MP4 / M3U8 流；
- 多个 M3U8 候选流出现时，通过切片数量探测优先选择切片最多的主视频流，降低下载到广告流的概率；
- 支持传统 TS 切片；
- 支持 fMP4 / `EXT-X-MAP` 初始化片段；
- 支持基础 AES-CBC HLS 解密；
- 支持多画质 Variant Playlist 自动选择最高带宽流；
- 支持高速/稳定两种模式：
  - 高速模式默认 30 并发，超时较短、重试较少；
  - 稳定模式默认 5 并发，带随机延迟、更多重试和退避；
- UI 中有“切片并发数”输入框，任务入队时会保存当时的并发数；
- 队列任务完成后弹出批次结果对话框；
- 失败任务可从结果对话框中重试；
- 切片失败数量按比例容忍：`失败数 ÷ 总数 ≤ 3%` 时仍判定任务可继续合并；
- 下载阶段和合并阶段都复用同一套 3% 缺片容忍规则；
- 结果对话框已适配当前整体 UI 的明暗主题，避免白底白字。

当前主要限制：

- 没有大平台专用解析器；
- 不支持 DASH / MPD；
- 不支持 DRM；
- 不完整继承浏览器 Cookie、LocalStorage、Authorization 等会话信息；
- 对强登录、验证码、地区限制、严格请求头校验的网站不稳定；
- HLS 多 Key、每切片 IV、`EXT-X-BYTERANGE`、多音轨/字幕、直播滚动 playlist 等复杂场景尚未补齐。

### 4.2 视频子目录提取

文件：`tools/video_extractor.py`

扫描指定目录及其子目录，将深层目录中的视频文件移动到根目录，带重名保护。可选清理空目录。

### 4.3 关键字归档

文件：`tools/keyword_organizer.py`

输入关键字后，扫描目录，把文件名包含关键字的图片/视频移动到对应归档文件夹。匹配成功时才创建目录。

### 4.4 图片智能裁剪

文件：`tools/image_resizer.py`

基于 Pillow 批量处理图片，支持自定义输出分辨率、透明通道转白底 RGB、高质量缩放和居中/保头/保脚裁剪策略。

## 5. 最近已完成的重要改动

以下是最近几轮对视频爬虫和主题相关的实际代码成果：

1. 增加切片并发数输入框；
2. 高速/稳定模式切换时分别设置默认并发 30 / 5；
3. 队列任务保存 URL、名称、保存目录、模式和并发数快照；
4. 等待队列全部处理完成后统一弹出批次结果；
5. 失败任务允许一键重试；
6. 下载器由“只打印错误”改为抛出明确 `VideoDownloadError`，方便队列收集失败原因；
7. 临时文件清理失败不会覆盖真正的下载/合并失败原因；
8. 切片失败容忍规则改为精确比例：`失败数 ÷ 总数 ≤ 3%` 判定可继续；
9. 合并前发现缺片也使用同样 3% 规则；
10. 修复队列结果弹窗在白底白字时不可读的问题；
11. 增加 `tests/test_video_downloader.py` 和 `tests/test_theme_utils.py` 覆盖上述行为；
12. 新增 `plan/video-crawler-roadmap.md`，只记录未来路线，没有实现其中功能。

## 6. 尚未实现但已规划的功能

详细路线见 `plan/video-crawler-roadmap.md`。优先级建议如下：

1. 诊断模式：输入 URL 后先报告是否发现 MP4/M3U8/MPD、是否疑似 DRM、失败原因是什么；
2. 浏览器会话继承：将 Playwright 捕获的 Cookie、Referer、Origin、User-Agent、Authorization 等安全地传给下载阶段；
3. HLS 边界补强：多 Key、IV、BYTERANGE、DISCONTINUITY、多音轨/字幕；
4. 结构化错误分类：例如 `NO_MEDIA_FOUND`、`UNSUPPORTED_DASH`、`HTTP_FORBIDDEN`、`FFMPEG_FAILED`；
5. 断点续传与切片级恢复；
6. 抽离适配器架构：Direct MP4、HLS、网页嗅探、DASH、站点适配器；
7. DASH / MPD 独立适配器；
8. 可选接入 yt-dlp 作为公开平台后备引擎。

重要：上一轮用户已经明确说明“目前只写计划书，不修改代码”。因此 `plan/video-crawler-roadmap.md` 中的诊断模式、会话继承、DASH 等都只是规划，还没有落地到代码。

## 7. 测试现状

现有测试文件：

- `tests/test_video_downloader.py`
- `tests/test_theme_utils.py`

常用测试命令：

```powershell
$env:QT_QPA_PLATFORM='offscreen'
$env:PYTHONDONTWRITEBYTECODE='1'
python -m unittest discover -s tests -v
```

之前在正常 Git 状态下，全量测试为 17 个测试通过。后续新对话如果继续开发，应先跑基线测试，再按 TDD 增量修改。

## 8. 当前工作区和仓库注意事项

新对话开始后请先检查 Git 状态。最近一次检查时出现异常：

```text
git log / git status 报错：fatal: bad object HEAD
.git/HEAD 指向 refs/heads/main
.git/refs/heads/main 指向 3a5fdd21ee5712e3636be4184c49600e5ce1c3fb
git cat-file 无法读取该对象
```

这说明当前 `.git` 元数据可能有损坏或引用指向了缺失对象。在修复前，不建议继续做提交、重置、清理 Git 对象等操作。建议新对话第一件事先只读检查：

```powershell
Get-Content .git\HEAD
Get-Content .git\refs\heads\main
git status
git log --oneline -5
git fsck --full
```

另外，之前还发现：

- `.git/objects` 目录异常大，约 10GB 级别；
- 存在多个超大 loose object，疑似历史中曾进入过视频/下载产物；
- `tools/__pycache__/*.pyc` 目前被 Git 跟踪，长期看应从仓库移除并加入 `.gitignore`；
- `.venv/` 本地存在但未被 Git 跟踪；
- `.idea/` 中多项 IDE 配置被 Git 跟踪；
- `tools/video_downloader-0.1.py` 是旧版备份文件；
- `tools/ffmpeg.exe` 约 95MB，当前被 Git 跟踪；
- `tools/downloads/` 和 `tools/temp/` 目录如果为空，可视为运行时目录。

## 9. 后续开发建议

如果新对话继续做代码开发，建议流程：

1. 先修复或确认 Git HEAD 状态；
2. 读取本文件、`plan/video-crawler-roadmap.md` 和相关源码；
3. 只做一个小阶段，例如“诊断模式 + UI 入口”；
4. 先写设计文档，再写实施计划；
5. 使用隔离 worktree；
6. 使用 TDD：先写失败测试，再写实现；
7. 每一步都跑对应测试；
8. 避免把下载产物、缓存、虚拟环境、临时文件提交进仓库。

## 10. 给新对话的简短提示词

如果要在新对话中快速接续，可以这样说：

```text
请先读取 D:\Study\Projects\PythonProject\FireflyTools\项目介绍.md。
这是一个 PyQt6 媒体工具箱项目，重点模块是 tools/video_downloader.py。
目前只需要基于项目介绍继续工作；开始前请先检查 Git HEAD 是否正常，不要贸然重置或删除文件。
```
